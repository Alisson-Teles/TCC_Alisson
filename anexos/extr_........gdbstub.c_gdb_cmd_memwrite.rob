
type kgdb_state{
	dummy = int32(0);
}


int32 error_packet(int32 _un0, int32 _un1);
remcom_out_buffer = int32(0);

int32 strcpy(int32 _un2, char _un3);

int32 write_mem_msg(int32 _un4);

void gdb_cmd_memwrite(kgdb_state ks) {
	err = int32(write_mem_msg(0));

	if err != 0 {
		error_packet(remcom_out_buffer, err);
	} else {
		strcpy(remcom_out_buffer, "OK");
	}

}

